spring.application.name=telegram

# URL del primer microservicio (KPIs)
feign.client.config.kpi-service.url=http://localhost:8080

# Token del bot
telegram.bot.token=TOKEN

# Puerto que utiliza el microservicio de Telegram
server.port=8081
